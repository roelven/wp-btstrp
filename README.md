# wp-btstrp: Bootstrap a Wordpress site

When cloning this into a new project, use

`git clone --recursive git@github.com:roelven/wp-btstrp.git`

so Git also clones the submodule repo in the same command.

